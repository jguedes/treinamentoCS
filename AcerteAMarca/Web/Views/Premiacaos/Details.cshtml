@model Dominio.Premiacao

@{
    ViewBag.Title = "Details";
}

<h2>Details</h2>

<div>
    <h4>Premiacao</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.ObjetoPremio)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.ObjetoPremio)
        </dd>
        <dt>
            @Html.DisplayNameFor(model => model.Regras)
            @Html.ActionLink("Create", "Create", "Regras", new { premiacaoId = Model.Id }, null)
        </dt>
        <dd>
            <table>
                @foreach (var item in Model.Regras)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.Descricao)
                        </td>
                        <td>
                            @Html.ActionLink("Edit", "Edit", "Regras",new { id = item.Id },null) |
                            @Html.ActionLink("Details", "Details", new { id = item.Id }) |
                            @Html.ActionLink("Delete", "Delete", new { id = item.Id })
                        </td>
                    </tr>
                }
            </table>
        </dd>
    </dl>
</div>
<p>
    @Html.ActionLink("Edit", "Edit", new { id = Model.Id }) |
    @Html.ActionLink("Back to List", "Index")
</p>
